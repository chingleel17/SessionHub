---
description: Run `bun run tauri build` in the current project
---

Run `bun run tauri build` from the repository root immediately.

Requirements:
- Do not ask for a plan.
- Do not make code changes.
- Do not inspect unrelated files.
- Use the bash tool to run exactly `bun run tauri build` in the project root.
- Stream the result back concisely.
- If the build fails, report the failing step and the relevant error output.
