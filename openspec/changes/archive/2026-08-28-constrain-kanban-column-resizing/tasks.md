## 1. 欄寬資料與版面基礎

- [x] 1.1 在 `DashboardView.tsx` 集中定義欄位數量、平均比例、最小比例與分隔線寬度，實作欄寬驗證／正規化純函式，並以合法、缺欄、非有限值、低於最小值及總和失衡案例驗證輸出
- [x] 1.2 將看板欄位改為依 flex 權重分配扣除固定分隔線後的空間，確認四個 25% 欄位與三條分隔線在不同視窗寬度下皆完整位於容器內且無水平捲動

## 2. 安全的欄寬互動

- [x] 2.1 修改拖曳計算，使移動量以扣除分隔線後的可用寬度換算，且只在相鄰兩欄間重新分配比例；拖到左右極限時驗證每欄至少 10%、總和維持 100% 且分隔線不會離開容器
- [x] 2.2 讓欄寬載入與儲存都通過相同驗證，手動放入異常 `sessionhub.kanban.columnWidths` 後重新開啟看板，確認自動恢復並持久化 `[25, 25, 25, 25]`
- [x] 2.3 為每條分隔線加入雙擊自動平均操作，先建立非平均欄寬再雙擊任一分隔線，確認四欄立即恢復 25% 且重新開啟看板後仍維持平均

## 3. 整合驗證

- [x] 3.1 驗證拖曳、雙擊、視窗縮放與側邊欄切換的組合流程，確認看板不出現水平捲動、視覺溢出或無法操作的分隔線
- [x] 3.2 執行 `bun run lint` 與 `bun run build`，確認前端 lint、TypeScript 型別檢查及正式版建置皆成功
